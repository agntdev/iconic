[
  {
    "name": "Feedback shows empty state when no icons exist",
    "steps": [
      {
        "send": { "callback": "feedback:submit" },
        "expect": [
          {
            "method": "sendMessage",
            "payload": {
              "text": "No icons to rate yet — tap 🎨 Generate to create some first!"
            }
          }
        ]
      }
    ]
  }
]
